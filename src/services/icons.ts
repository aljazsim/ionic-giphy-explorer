import { closeOutline, copyOutline, ellipsisHorizontal, ellipsisVertical, openOutline, saveOutline, searchOutline, syncOutline } from "ionicons/icons";

export class Icons
{
    public readonly clear = closeOutline;
    public readonly copy = copyOutline;
    public readonly ellipsisHorizontal = ellipsisHorizontal;
    public readonly ellipsisVertical = ellipsisVertical;
    public readonly loading = syncOutline;
    public readonly open = openOutline;
    public readonly save = saveOutline;
    public readonly search = searchOutline;
}