import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'mobile.giphy.explorer',
    appName: 'Mobile Giphy Eplorer',
    webDir: 'dist',
    bundledWebRuntime: false
};

export default config;
